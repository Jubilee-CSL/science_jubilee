# Host environment â€” notebook + post-processing steps that run locally
# COLMAP and 3DGS training run in Docker (see docker-compose.yml)

matplotlib

# Point cloud & mesh processing
# opencv-contrib required for cv2.aruco used in filter_scene.py
numpy
open3d
opencv-contrib-python-headless

# Image processing
Pillow
plyfile
# Notebook / pipeline orchestration
sacred
scipy

# AI segmentation (filter_scene.py â€” BiRefNet via transformers)
# GPU: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# CPU: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch
torchvision
transformers
ultralytics
